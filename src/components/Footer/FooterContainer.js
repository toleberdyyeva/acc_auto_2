import { FooterLayout } from "./FooterLayout";
import React from "react";

export const FooterContainer = (props) => {
  return (
    <React.Fragment>
      <FooterLayout />
    </React.Fragment>
  );
};
